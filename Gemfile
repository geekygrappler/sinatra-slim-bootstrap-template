# A Gemfile will ensure you have all the gems (ruby ma gic) that this application needs
# are installed on your machine, or when you run it somewhere else, e.g. on Heroku
# the Gemfile will be used to install all the gems on another computer in the cloud

source "https://rubygems.org"
ruby "2.0.0"

gem "sinatra"